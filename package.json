{
  "name": "pareto",
  "version": "0.19.0",
  "description": "javascript/typescript promises with typesafe error handling in a functional style",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "u2l": "ncu -u --packageFile package.json && npm install",
    "pubMin": "rd /s /q .\\dist && tsc && npm run lint && npm version minor&& git push && npm publish",
    "lint": "tslint --project tsconfig.json ./src/*",
    "build": "tsc"
  },
  "keywords": [],
  "author": "corno",
  "license": "ISC",
  "types": "dist/index.d.ts",
  "prepublish": "tsc",
  "repository": {
    "type": "git",
    "url": "https://github.com/corno/pareto.git"
  },
  "files": [
    "dist/*"
  ],
  "dependencies": {
    "pareto-api": "^0.6.0"
  },
  "devDependencies": {
    "@types/node": "^12.11.2",
    "steroid-template-utils": "^0.2.0"
  }
}
