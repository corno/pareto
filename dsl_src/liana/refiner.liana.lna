(
    `schema`: | `schema` (
        `schema imports`: {}
        `resolver imports`: {}
        `globals`: (
            `complexity`: | `unconstrained` ~
            `text types`: {
                'message': < | `single line` ~ | `no` ~ >
                'pseudo reference': < | `single line` ~ | `no` ~ >
                'text literal': < | `single line` ~ | `no` ~ >
                'reference literal': < | `single line` ~ | `no` ~ >
            }
            `simple types`: {
                'boolean': < | `boolean` ~ >
                'natural literal': < | `number` < | `exact` < _ | `natural` ~ > > >
                'integer literal': < | `number` < | `exact` < _ | `integer` ~ > > >
            }
        )
        `modules`: {
            'Expression': < | `state` < {
                'abort': < _ _ | `component` < | `internal` 'Expression' _ > >
                'change context': < _ _ | `group` {
                    'select': < _ | `component` < | `internal acyclic` 'Select Value' _ > >
                    'callback': < _ | `component` < | `internal` 'Expression' _ > >
                } >
                'convert': < _ _ | `group` {
                    'select': < _ | `component` < | `internal acyclic` 'Select Value' _ > >
                    'type': < _ | `state` < {
                        'component': < _ _ | `state` < {
                            'transform': < _ _ | `group` {
                                'scope': < _ | `state` < {
                                    'external': < _ _ | `group` {
                                        'refiner': < _ | `text` | `global` 'pseudo reference' >
                                        'function': < _ | `text` | `global` 'pseudo reference' >
                                    } >
                                    'local': < _ _ | `group` {
                                        'function': < _ | `text` | `global` 'pseudo reference' >
                                    } >
                                } _ > >
                                'error': < _ | `state` < {
                                    'omit': < _ _ | `nothing` ~ >
                                    'pass through': < _ _ | `nothing` ~ >
                                    'handle': < _ _ | `component` < | `internal` 'Expression' _ > >
                                } _ > >
                                'arguments': < _ | `state` < {
                                    'omit': < _ _ | `nothing` ~ >
                                    'pass through': < _ _ | `nothing` ~ >
                                    'initialize': < _ _ | `component` < | `internal` 'Expression' _ > >
                                } _ > >
                            } >
                        } _ > >
                        'dictionary': < _ _ | `state` < {
                            'map': < _ _ | `group` {
                                'on entry': < _ | `component` < | `internal` 'Expression' _ > >
                            } >
                        } _ > >
                        'list': < _ _ | `state` < {
                            'map': < _ _ | `group` {
                                'on item': < _ | `component` < | `internal` 'Expression' _ > >
                            } >
                        } _ > >
                        'optional': < _ _ | `state` < {
                            'decide': < _ _ | `group` {
                                'temp return type': < _ | `optional` | `component` < | `internal acyclic` 'Value Reference' _ > >
                                'on set': < _ | `component` < | `internal` 'Expression' _ > >
                                'on not set': < _ | `component` < | `internal` 'Expression' _ > >
                            } >
                            'map': < _ _ | `group` {
                                'on set': < _ | `component` < | `internal` 'Expression' _ > >
                            } >
                        } _ > >
                        'state': < _ _ | `state` < {
                            'decide': < _ _ | `group` {
                                'temp return type': < _ | `optional` | `component` < | `internal acyclic` 'Value Reference' _ > >
                                'options': < _ | `dictionary` < | `component` < | `internal` 'Expression' _ > > >
                            } >
                        } _ > >
                        'text': < _ _ | `state` < {
                            'decide': < _ _ | `group` {
                                'temp return type': < _ | `optional` | `component` < | `internal acyclic` 'Value Reference' _ > >
                                'matches': < _ | `dictionary` < | `component` < | `internal` 'Expression' _ > > >
                                'no match': < _ | `component` < | `internal` 'Expression' _ > >
                            } >
                        } _ > >
                    } _ > >
                } >
                'implement me': < _ _ | `group` {
                    'remark': < _ | `text` | `global` 'message' >
                } >
                'initialize': < _ _ | `state` < {
                    'boolean': < _ _ | `state` < {
                        'false': < _ _ | `nothing` ~ >
                        'true': < _ _ | `nothing` ~ >
                    } _ > >
                    'dictionary': < _ _ | `dictionary` < | `component` < | `internal` 'Expression' _ > > >
                    'group': < _ _ | `dictionary` < | `component` < | `internal` 'Expression' _ > > >
                    'list': < _ _ | `list` < | `component` < | `internal` 'Expression' _ > _ > >
                    'nothing': < _ _ | `nothing` ~ >
                    'number': < _ _ | `state` < {
                        'natural': < _ _ | `simple` | `global` 'natural literal' >
                        'integer': < _ _ | `simple` | `global` 'integer literal' >
                    } _ > >
                    'optional': < _ _ | `state` < {
                        'not set': < _ _ | `nothing` ~ >
                        'set': < _ _ | `component` < | `internal` 'Expression' _ > >
                    } _ > >
                    'reference': < _ _ | `text` | `global` 'reference literal' >
                    'state': < _ _ | `group` {
                        'option': < _ | `text` | `global` 'pseudo reference' >
                        'data': < _ | `component` < | `internal` 'Expression' _ > >
                    } >
                    'text': < _ _ | `text` | `global` 'text literal' >
                } _ > >
                'select': < _ _ | `component` < | `internal acyclic` 'Select Value' _ > >
            } _ > >
            'Root': < | `group` {
                'development mode': < _ | `simple` | `global` 'boolean' >
                'source schema': < _ | `component` < | `internal acyclic` 'Schema Reference' _ > >
                'error schema': < _ | `optional` | `component` < | `internal acyclic` 'Schema Reference' _ > >
                'parameters schema': < _ | `optional` | `component` < | `internal acyclic` 'Schema Reference' _ > >
                'dependencies': < _ | `group` {
                    'deserializers': < _ | `dictionary` < | `state` < {
                        'this schema': < _ _ | `nothing` ~ >
                        'cousin': < _ _ | `group` {
                            'schema': < _ | `text` | `global` 'pseudo reference' >
                        } >
                        'external': < _ _ | `group` {
                            'package': < _ | `text` | `global` 'pseudo reference' >
                            'module': < _ | `text` | `global` 'pseudo reference' >
                            'schema': < _ | `text` | `global` 'pseudo reference' >
                        } >
                    } _ > > >
                    'refiners': < _ | `dictionary` < | `state` < {
                        'sibling': < _ _ | `group` {
                            'refiner': < _ | `text` | `global` 'pseudo reference' >
                        } >
                        'cousin': < _ _ | `group` {
                            'schema': < _ | `text` | `global` 'pseudo reference' >
                            'refiner': < _ | `text` | `global` 'pseudo reference' >
                        } >
                        'external': < _ _ | `group` {
                            'package': < _ | `text` | `global` 'pseudo reference' >
                            'module': < _ | `text` | `global` 'pseudo reference' >
                            'schema': < _ | `text` | `global` 'pseudo reference' >
                            'refiner': < _ | `text` | `global` 'pseudo reference' >
                        } >
                    } _ > > >
                } >
                'declarations': < _ | `dictionary` < | `group` {
                    'source': < _ | `state` < {
                        'value': < _ _ | `component` < | `internal acyclic` 'Value Reference' _ > >
                        'iterator': < _ _ | `component` < | `internal acyclic` 'Value Reference' _ > >
                    } _ > >
                    'error': < _ | `optional` | `component` < | `internal acyclic` 'Type Reference' _ > >
                    'parameters': < _ | `optional` | `component` < | `internal acyclic` 'Type Reference' _ > >
                } > >
                'implementations': < _ | `dictionary` < | `group` {
                    'temp has error': < _ | `simple` | `global` 'boolean' >
                    'temp has parameters': < _ | `simple` | `global` 'boolean' >
                    'expression': < _ | `component` < | `internal acyclic` 'Expression' _ > >
                } > >
            } >
            'Schema Reference': < | `state` < {
                'sibling': < _ _ | `text` | `global` 'pseudo reference' >
                'external': < _ _ | `group` {
                    'package': < _ | `text` | `global` 'pseudo reference' >
                    'module': < _ | `text` | `global` 'pseudo reference' >
                    'schema': < _ | `text` | `global` 'pseudo reference' >
                } >
            } _ > >
            'Type Reference': < | `group` {
                'type': < _ | `text` | `global` 'pseudo reference' >
            } >
            'Value Reference': < | `group` {
                'type': < _ | `text` | `global` 'pseudo reference' >
                'subselection': < _ | `list` < | `state` < {
                    'dictionary': < _ _ | `nothing` ~ >
                    'group': < _ _ | `group` {
                        'property': < _ | `text` | `global` 'pseudo reference' >
                    } >
                    'list': < _ _ | `nothing` ~ >
                    'optional': < _ _ | `nothing` ~ >
                    'state': < _ _ | `group` {
                        'option': < _ | `text` | `global` 'pseudo reference' >
                    } >
                } _ > _ > >
            } >
            'Select Value': < | `state` < {
                'call': < _ * "this one should only be used to call functions that only select" | `group` {
                    'scope': < _ | `state` < {
                        'external': < _ _ | `group` {
                            'refiner': < _ | `text` | `global` 'pseudo reference' >
                            'function': < _ | `text` | `global` 'pseudo reference' >
                        } >
                        'local': < _ _ | `group` {
                            'function': < _ | `text` | `global` 'pseudo reference' >
                        } >
                    } _ > >
                    'context': < _ | `component` < | `internal` 'Select Value' _ > >
                    'error': < _ | `state` < {
                        'omit': < _ _ | `nothing` ~ >
                        'pass through': < _ _ | `nothing` ~ >
                        'handle': < _ _ | `group` {
                            'expression': < _ | `component` < | `internal` 'Expression' _ > >
                        } >
                    } _ > >
                    'arguments': < _ | `state` < {
                        'omit': < _ _ | `nothing` ~ >
                        'pass through': < _ _ | `nothing` ~ >
                        'initialize': < _ _ | `group` {
                            'expression': < _ | `component` < | `internal` 'Expression' _ > >
                        } >
                    } _ > >
                    'tail': < _ | `component` < | `internal acyclic` 'Value Selection Tail' _ > >
                } >
                'context value': < _ _ | `group` {
                    'tail': < _ | `component` < | `internal acyclic` 'Value Selection Tail' _ > >
                } >
            } _ > >
            'Value Selection Tail': < | `list` < | `text` | `global` 'pseudo reference' _ > >
        }
        `complexity`: | `unconstrained` ~
    )
    `schema path`: [ ]
    `complexity`: | `unconstrained` (
        `module`: "Root"
    )
)