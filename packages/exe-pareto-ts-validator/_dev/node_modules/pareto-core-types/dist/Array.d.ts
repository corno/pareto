import { AsyncValue } from "./AsyncValue";
export type Array<T> = {
    readonly "forEach": ($i: ($: T) => void) => void;
    readonly "map": <NT>($v: ($: T) => NT) => Array<NT>;
    readonly "filter": <NT>($v: ($: T) => NT | undefined) => Array<NT>;
    readonly "reduce": <NT>($: NT, $v: (current: NT, $: T) => NT) => NT;
    readonly "asyncMap": <NT>($v: ($: T) => AsyncValue<NT>) => AsyncValue<Array<NT>>;
};
