{
  "name": "pareto-utils",
  "version": "0.14.0",
  "description": "javascript/typescript promises with typesafe error handling in a functional style",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "updateToLatest": "ncu -u --packageFile package.json && npm install",
    "publishAsMinor": "npm version minor && npm publish && git push"
  },
  "keywords": [],
  "author": "corno",
  "license": "ISC",
  "types": "dist/index.d.ts",
  "prepublish": "tsc",
  "repository": {
    "type": "git",
    "url": "https://github.com/corno/pareto-utils.git"
  },
  "files": [
    "dist/*"
  ],
  "dependencies": {
    "pareto-api": "^0.6.0"
  },
  "devDependencies": {
    "@types/node": "^12.11.1",
    "steroid-template-utils": "^0.1.5"
  }
}
