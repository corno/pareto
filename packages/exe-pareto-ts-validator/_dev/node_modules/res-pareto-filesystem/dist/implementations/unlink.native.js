"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.$$ = void 0;
const pi = __importStar(require("pareto-core-internals"));
const joinPath_native_1 = require("../native/joinPath.native");
const unlinkImp_native_1 = require("../native/unlinkImp.native");
const $$ = ($) => {
    return pi.wrapAsyncValueImp(true, (cb) => {
        const joinedPath = (0, joinPath_native_1.joinPath)($.path);
        (0, unlinkImp_native_1.unlinkImp)((0, joinPath_native_1.joinPath)($.path), (err) => {
            if (err !== null) {
                const errCode = err.code;
                const errMessage = err.message;
                function createError() {
                    switch (errCode) {
                        case 'ENOENT':
                            return ['no entity', null];
                        default: {
                            console.log(`CORE: DEV TODO: ADD THIS OPTION TO pareto-filesystem UNLINK: ${errMessage}`);
                            return ['unknown', { message: errMessage }];
                        }
                    }
                }
                cb(['error', {
                        error: createError(),
                        path: joinedPath,
                    }]);
            }
            else {
                cb(['success', null]);
            }
        });
    });
};
exports.$$ = $$;
